// https://stackoverflow.com/questions/10956740/finding-latest-modified-file-in-a-folder

function FindLatestFile(dir) {
    for (files in dir){
        
    }
}


function UpdateImage(dir) {
    var FolderName = "../images/"

    FindLatestFile(FolderName);

    /*
    var ImageLoc = document.getElementsByClassName("ImageCaptured");
    ImageLoc.src = "../images/" + FileName;
    */
};